export interface RawCSVRow {
  product_name: string;
  brand: string;
  category: string;
  barcode: string;
  price: string;
  stock: string;
  size?: string;
  color?: string;
  description?: string;
}
